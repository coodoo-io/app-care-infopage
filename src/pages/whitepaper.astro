---
import BaseLayout from '../layouts/BaseLayout.astro';

const topics = [
  { icon: '👥', title: 'Engagement KPIs', desc: 'MAU, DAU, Stickiness, Retention, Churn — welche wirklich zählen' },
  { icon: '⚡', title: 'Performance KPIs', desc: 'Crash Rate, Loading Speed, ANR Rate — Stabilität messen' },
  { icon: '💰', title: 'Revenue KPIs', desc: 'ARPU, LTV, Subscription Churn — Monetarisierung optimieren' },
  { icon: '⭐', title: 'App Store KPIs', desc: 'Ratings, Conversions, Installationen — Sichtbarkeit steigern' },
];
---

<BaseLayout
  title="Whitepaper: Mobile App Erfolg durch KPIs | App-Care"
  description="Kostenloses Whitepaper: Alle wichtigen KPIs für Mobile Apps, Benchmark-Werte und Handlungsempfehlungen. Jetzt kostenlos herunterladen."
  noindex={false}
>
  <!-- Minimal header for conversion focus -->
  <style>
    header { display: none; }
    footer { display: none; }
  </style>

  <div class="min-h-screen bg-stone-50 flex flex-col">
    <!-- Minimal Nav -->
    <div class="bg-white border-b border-stone-100 py-4">
      <div class="max-w-5xl mx-auto px-4 flex items-center justify-between">
        <a href={import.meta.env.BASE_URL} class="flex items-center gap-2">
          <div class="w-7 h-7 bg-brand-600 rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-xs">AC</span>
          </div>
          <span class="font-display font-bold text-lg text-stone-900">App-Care</span>
        </a>
        <span class="text-sm text-stone-400">Kostenlos & ohne Risiko</span>
      </div>
    </div>

    <main class="flex-1">
      <!-- Hero -->
      <section class="bg-stone-50 border-b border-stone-100 py-20 md:py-24">
        <div class="max-w-5xl mx-auto px-4">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div>
              <span class="badge-gray mb-4 inline-block">Kostenlos herunterladen</span>
              <h1 class="font-display font-bold text-4xl md:text-5xl text-stone-900 mb-6 leading-tight">
                Mobile App Erfolg durch KPIs
              </h1>
              <p class="text-stone-500 text-lg leading-relaxed mb-6">
                Über 25 KPIs aus 4 Kategorien — was sie bedeuten, wie du sie misst und welche Benchmarks gelten. Kompakt als PDF, sofort nutzbar.
              </p>
              <div class="flex flex-wrap gap-3 mb-6">
                {topics.map((t) => (
                  <div class="flex items-center gap-2 text-sm text-stone-500">
                    <span>{t.icon}</span>
                    <span>{t.title}</span>
                  </div>
                ))}
              </div>
            </div>

            <div class="bg-stone-900 rounded-xl p-8 border border-stone-800">
              <h2 class="font-display font-bold text-2xl text-white mb-2">Jetzt kostenlos herunterladen</h2>
              <p class="text-stone-400 text-sm mb-6">Gib deine E-Mail-Adresse ein und erhalte das Whitepaper sofort.</p>

              <form
                action="https://formspree.io/f/placeholder"
                method="POST"
                class="space-y-4"
              >
                <div>
                  <label for="name" class="block text-sm font-medium text-stone-400 mb-1">Dein Name</label>
                  <input
                    type="text"
                    id="name"
                    name="name"
                    placeholder="Max Mustermann"
                    required
                    class="w-full px-4 py-3 rounded-lg bg-stone-800 border border-stone-700 text-white placeholder-stone-500 focus:outline-none focus:ring-2 focus:ring-brand-500/50"
                  />
                </div>
                <div>
                  <label for="email" class="block text-sm font-medium text-stone-400 mb-1">Deine E-Mail-Adresse</label>
                  <input
                    type="email"
                    id="email"
                    name="email"
                    placeholder="max@meinestartup.de"
                    required
                    class="w-full px-4 py-3 rounded-lg bg-stone-800 border border-stone-700 text-white placeholder-stone-500 focus:outline-none focus:ring-2 focus:ring-brand-500/50"
                  />
                </div>
                <input type="hidden" name="_subject" value="Whitepaper Download Anfrage" />
                <input type="hidden" name="_next" value="/whitepaper/danke" />
                <button
                  type="submit"
                  class="w-full py-4 bg-brand-500 text-white font-semibold rounded-lg hover:bg-brand-600 transition-colors text-lg"
                >
                  Whitepaper kostenlos herunterladen →
                </button>
                <p class="text-xs text-stone-500 text-center">
                  Kein Spam. Keine Weitergabe. Jederzeit abmeldbar.
                </p>
              </form>
            </div>
          </div>
        </div>
      </section>

      <!-- Content Preview -->
      <section class="py-16 bg-white">
        <div class="max-w-5xl mx-auto px-4">
          <h2 class="font-display font-bold text-2xl text-stone-900 text-center mb-10">
            Was steht im Whitepaper?
          </h2>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {topics.map((topic) => (
              <div class="card border border-stone-100">
                <div class="flex items-start gap-4">
                  <span class="text-3xl">{topic.icon}</span>
                  <div>
                    <h3 class="font-display font-semibold text-stone-900 mb-1">{topic.title}</h3>
                    <p class="text-stone-600 text-sm">{topic.desc}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>

          <div class="mt-10 bg-stone-50 rounded-xl p-8 text-center">
            <p class="text-stone-600 mb-4">
              <strong class="text-stone-900">Plus:</strong> Formeln, Benchmark-Werte, Daten-Quellen und konkrete Handlungsempfehlungen für jeden KPI.
            </p>
            <p class="text-sm text-stone-500">Ca. 20 Seiten · PDF · Kostenlos</p>
          </div>
        </div>
      </section>

      <!-- About -->
      <section class="py-12 bg-stone-50 border-t border-stone-100">
        <div class="max-w-3xl mx-auto px-4 text-center">
          <p class="text-stone-600 text-sm leading-relaxed">
            Das Whitepaper wurde von <strong>Markus Kühle</strong>, Geschäftsführer der coodoo GmbH, auf Basis von 20+ Jahren Berufserfahrung und hunderten monatlichen App-Reports erstellt.
            <a href={import.meta.env.BASE_URL} class="text-brand-500 hover:underline ml-1">Mehr über App-Care →</a>
          </p>
        </div>
      </section>
    </main>
  </div>
</BaseLayout>
