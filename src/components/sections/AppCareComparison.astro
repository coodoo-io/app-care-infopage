---
const without = [
  'Veraltete Dependencies häufen sich unbemerkt an',
  'iOS & Android Updates brechen unerwartete Features',
  'Nutzerbewertungen sinken — niemand reagiert',
  'Kritische Bugs bleiben tagelang unentdeckt',
  'Store-Listings veralten und verlieren Sichtbarkeit',
  'Kein Überblick über KPIs und Nutzungsverhalten',
];

const with_care = [
  'Regelmäßige Dependency- und Framework-Updates',
  'Sofortige Reaktion bei kritischen Problemen',
  'Aktives Review-Monitoring und Antworten',
  'Monatlicher KPI-Report mit klaren Handlungsempfehlungen',
  'Kontinuierliche Store-Optimierung',
  'Zwei feste Ansprechpartner — immer im Bild',
];
---

<section class="bg-gray-950 text-white section-padding">
  <div class="section-container">

    <div class="text-center max-w-2xl mx-auto mb-12">
      <span class="badge bg-white/10 text-gray-300 mb-4 inline-block">Die Realität nach dem Launch</span>
      <h2 class="font-display font-bold text-3xl md:text-4xl text-white mb-4">
        Nach dem Launch beginnt<br />die eigentliche Arbeit.
      </h2>
      <p class="text-gray-400 text-lg">
        Die meisten denken, eine App ist fertig wenn sie live ist. Dabei entscheidet sich erst danach, ob sie wächst — oder still stirbt.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-4xl mx-auto">

      <!-- Ohne Betreuung -->
      <div class="bg-white/5 border border-white/10 rounded-2xl p-6">
        <div class="flex items-center gap-3 mb-6">
          <div class="w-8 h-8 rounded-full bg-red-500/20 flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 text-red-400" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
            </svg>
          </div>
          <h3 class="font-display font-bold text-white">Ohne Betreuung</h3>
        </div>
        <ul class="space-y-3">
          {without.map((item) => (
            <li class="flex items-start gap-3">
              <svg class="w-4 h-4 text-red-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12" />
              </svg>
              <span class="text-gray-400 text-sm leading-relaxed">{item}</span>
            </li>
          ))}
        </ul>
      </div>

      <!-- Mit App-Care -->
      <div class="bg-brand-600/20 border border-brand-500/30 rounded-2xl p-6">
        <div class="flex items-center gap-3 mb-6">
          <div class="w-8 h-8 rounded-full bg-accent-500/20 flex items-center justify-center flex-shrink-0">
            <svg class="w-4 h-4 text-accent-400" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
            </svg>
          </div>
          <h3 class="font-display font-bold text-white">Mit App-Care</h3>
        </div>
        <ul class="space-y-3">
          {with_care.map((item) => (
            <li class="flex items-start gap-3">
              <svg class="w-4 h-4 text-accent-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
              </svg>
              <span class="text-gray-200 text-sm leading-relaxed">{item}</span>
            </li>
          ))}
        </ul>
      </div>

    </div>
  </div>
</section>
