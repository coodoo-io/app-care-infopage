---
---

<section class="section-padding bg-white">
  <div class="section-container">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="section-title mb-4">
        Bereit, deine App auf das nächste Level zu bringen?
      </h2>
      <p class="section-subtitle mb-10">
        Lass uns in einem kostenlosen 30-Minuten-Gespräch schauen, wie App-Care deiner App helfen kann. Kein Pitch. Kein Druck. Nur ehrliche Beratung.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-8">
        <a href={`${import.meta.env.BASE_URL.replace(/\/?$/, '')}/kontakt`} class="btn-primary text-lg px-8 py-4">
          Kostenloses Gespräch buchen
        </a>
        <a href={`${import.meta.env.BASE_URL.replace(/\/?$/, '')}/pakete`} class="btn-secondary text-lg px-8 py-4">
          Pakete vergleichen
        </a>
      </div>

      <div class="flex items-center justify-center gap-8 text-sm text-gray-500 mb-10">
        <span class="flex items-center gap-1">✓ Kostenlos & unverbindlich</span>
        <span class="flex items-center gap-1">✓ In 48h Antwort</span>
        <span class="flex items-center gap-1">✓ Kein Risiko</span>
      </div>

      <div class="flex items-center justify-center gap-4 bg-gray-50 rounded-2xl px-6 py-4 max-w-sm mx-auto">
        <img
          src={`${import.meta.env.BASE_URL.replace(/\/?$/, '')}/assets/Markus.jpg`}
          alt="Markus Kühle"
          class="w-12 h-12 rounded-full object-cover flex-shrink-0"
        />
        <div class="text-left">
          <p class="font-semibold text-gray-900 text-sm leading-tight">Markus Kühle</p>
          <p class="text-gray-500 text-xs">Geschäftsführer, coodoo GmbH · antwortet persönlich</p>
        </div>
      </div>
    </div>
  </div>
</section>
